name: Mypy testing

on:
  push:
  pull_request:

jobs:
  mypy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6

      - name: Install Dependencies
        run: make install

      - name: Test Typing
        run: make mypy_check
